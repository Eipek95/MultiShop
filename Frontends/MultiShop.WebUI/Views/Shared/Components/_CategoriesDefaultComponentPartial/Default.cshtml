@model List<CategoryProduct>


 
<div class="container-fluid pt-5">
    <h2 class="section-title position-relative text-uppercase mx-xl-5 mb-4"><span class="bg-secondary pr-3">Öne Çıkan Kategoriler</span></h2>
  


        @* <div class="row px-xl-5 pb-3">
        @foreach (var item in Model)
        {
            <div class="col-lg-3 col-md-4 col-sm-6 pb-1">
                <a class="text-decoration-none" asp-action="Index" asp-controller="ProductList" asp-route-id="@item.CategoryID">
                    <div class="cat-item d-flex align-items-center mb-4">
                        <div class="overflow-hidden" style="width: 100px; height: 100px;">
                            <img class="img-fluid" src="@item.ImageUrl" alt="">
                        </div>
                        <div class="flex-fill pl-3">
                            <h6>@item.CategoryName</h6>
                            <small class="text-body">@item.Count Ürün</small>
                        </div>
                    </div>
                </a>
            </div>
        }
        </div> *@
    <div class="row px-xl-5">
        <div class="col">
            <div class="owl-carousel vendor-carousel">

                @foreach (var product in Model)
                {
                    <div class="bg-light p-4">
                        <img src="@product.ImageUrl" alt="@product.CategoryName.ToLower().Replace(" ","-")" title="@product.CategoryName">

                        <a class="text-decoration-none" asp-action="Index" asp-controller="ProductList" asp-route-id="@product.CategoryID">
                            <div class="cat-item d-flex align-items-center mb-4 mt-3">
                                <div class="flex-fill pl-3">
                                    <h5 class="text-center">@product.CategoryName</h5>
                                    <hr />
                                    <h6 class="text-center">@product.Count Ürün</h6>
                                </div>
                            </div>
                        </a>
                    </div>
                }
            </div>
        </div>
    </div>
</div>